import styled from 'styled-components';
import { space, width, height, background } from 'styled-system';

const Image = styled.img`
  ${space}
  ${width}
  ${height}
  ${background}
`;

export default Image;
